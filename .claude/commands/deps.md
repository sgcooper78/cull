---
description: Sync or inspect Dart dependencies
argument-hint: "[get|upgrade|outdated]"
---

Run `flutter pub $ARGUMENTS` from the project root (default to `get` when no
argument is given). Show the result. For `outdated`, summarize which bumps look
safe (patch/minor, no breaking notes) and which need care.
