---
description: Run the app on Windows desktop
---

Run `flutter run -d windows` from the project root. If the build fails, show the
error and stop. Otherwise confirm it launched and leave it running.
